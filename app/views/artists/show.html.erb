<h1><%= @artist.name %></h1>
<p><%= pluralize(@artist.songs.count, 'Song') %> in <%= @artist.name %></p>
<ul>
  <% @artist.songs.each do |song| %>
  <li>
    <%= link_to song.title, song_path(song) %>
  </li>
  <% end %>
</ul>
